{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with Args",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": ["data/test.jpg"],
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb"
    }
  ]
}